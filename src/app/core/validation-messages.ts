export const VALIDATION_MESSAGES = {
  required: 'Required field',
  maxLenght: (length: number) => `This field must be a maximum of ${length} characters`,
  minLenght: (length: number) => `This field must be a minimum of ${length} characters`
};
